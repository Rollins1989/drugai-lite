const $ = (s) => document.querySelector(s);
const esc = (v) => String(v ?? '').replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
function badgeClass(label){const l=(label||'').toLowerCase();if(['good','high','pass','clear','high_agreement'].includes(l))return'badge-good';if(['moderate','moderate_agreement'].includes(l))return'badge-moderate';if(['poor','low','fail','flagged','low_agreement'].includes(l))return'badge-poor';return''}
function svgImg(b64){return `<img src="data:image/svg+xml;base64,${b64}" alt="2D molecular structure">`}
function metric(label,value,extra=''){return `<div class="metric"><div class="metric-label">${label}</div><div class="metric-value">${value} ${extra}</div></div>`}

document.querySelectorAll('.nav-btn').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.nav-btn').forEach(b=>b.classList.remove('active'));document.querySelectorAll('.view').forEach(v=>v.classList.remove('active'));btn.classList.add('active');$('#view-'+btn.dataset.view).classList.add('active');if(btn.dataset.view==='about')loadAbout()}));
document.querySelectorAll('.chip[data-smi]').forEach(chip=>chip.addEventListener('click',()=>{$('#smiles-input').value=chip.dataset.smi;$('#analyze-form').dispatchEvent(new Event('submit'))}));

$('#analyze-form').addEventListener('submit',async e=>{e.preventDefault();const smiles=$('#smiles-input').value.trim();if(!smiles)return;const out=$('#analyze-result');out.classList.remove('hidden');out.innerHTML='<div class="loading">Running RDKit + ensemble models + structural alerts + analog search…</div>';try{const r=await fetch('/api/analyze',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({smiles})});const d=await r.json();if(!r.ok){out.innerHTML=`<div class="error-box">${esc(d.detail||'Could not parse this SMILES.')}</div>`;return}renderAnalysis(d)}catch(err){out.innerHTML=`<div class="error-box">Request failed: ${esc(err)}</div>`}});

function renderAnalysis(d){const desc=d.descriptors,id=d.identity,domain=d.applicability_domain,score=d.screening_score;const agreement=(x)=>x?.model_agreement?`${x.model_agreement.level} · Δ ${x.model_agreement.absolute_disagreement}`:'—';const explain=[...(d.explanation.toxicity||[]).map(x=>`Toxicity · ${x.descriptor} = ${x.value} · importance ${x.importance}`),...(d.explanation.solubility||[]).map(x=>`Solubility · ${x.descriptor} = ${x.value} · importance ${x.importance}`)].slice(0,8);const descRows=Object.entries(desc).map(([k,v])=>`<div>${esc(k)}: <b>${esc(v)}</b></div>`).join('');const analogs=d.nearest_analogs.map(a=>`<div class="analog"><code>${esc(a.smiles)}</code><span>${esc(a.source)}</span><b>${a.tanimoto}</b></div>`).join('');const alerts=d.structural_alerts.alerts.length?d.structural_alerts.alerts.map(esc).join(', '):'No PAINS alerts detected';$('#analyze-result').innerHTML=`<div class="mol-card"><div><div class="mol-struct">${svgImg(d.structure_svg_b64)}</div><div class="score-grid"><div class="overall-score"><div class="num">${score.value}</div><div class="lbl">Heuristic screening score</div></div><div class="domain-score"><div class="metric-label">Chemical-space proximity</div><b class="domain-${domain.status.toLowerCase()}">${esc(domain.status)}</b><div class="tiny">Nearest Tanimoto: ${domain.nearest_tanimoto??'—'}</div></div></div><div class="identity"><div><span>Formula</span><b>${esc(id.formula)}</b></div><div><span>Canonical SMILES</span><code>${esc(id.canonical_smiles)}</code></div><div><span>Murcko scaffold</span><code>${esc(id.murcko_scaffold_smiles||'Acyclic')}</code></div></div><div class="section-label">Rule checks</div><div class="rule-row"><span>Lipinski</span><span class="badge ${badgeClass(d.lipinski.pass?'pass':'fail')}">${d.lipinski.pass?'PASS':'FAIL'}</span><span>Veber</span><span class="badge ${badgeClass(d.veber.pass?'pass':'fail')}">${d.veber.pass?'PASS':'FAIL'}</span><span>PAINS</span><span class="badge ${badgeClass(d.structural_alerts.pass?'clear':'flagged')}">${d.structural_alerts.pass?'CLEAR':'FLAGGED'}</span></div><div class="tiny alert-text">${esc(alerts)}</div></div><div><div class="metric-grid">${metric('Predicted solubility',`${d.solubility.log_solubility_mol_per_L} log mol/L`,`<span class="badge ${badgeClass(d.solubility.label)}">${d.solubility.label}</span>`)}${metric('Solubility model agreement',esc(agreement(d.solubility)))}${metric('Toxicity probability',`${(d.toxicity.toxicity_probability*100).toFixed(1)}%`,`<span class="badge ${badgeClass(d.toxicity.label)}">${d.toxicity.label}</span>`)}${metric('Toxicity model agreement',esc(agreement(d.toxicity)))}</div><div class="section-label">Model explanation</div><ul class="explain-list">${explain.map(esc).map(x=>`<li>${x}</li>`).join('')}</ul><div class="section-label">Nearest reference analogs</div><div class="analog-list">${analogs}</div><div class="section-label">Screening-score components</div><div class="desc-table">${Object.entries(score.components).map(([k,v])=>`<div>${esc(k)}: <b>${v}</b></div>`).join('')}</div><div class="tiny">${esc(score.note)}</div><div class="section-label">Molecular descriptors</div><div class="desc-table">${descRows}</div></div></div>`}

const sample=["CC(=O)Oc1ccccc1C(=O)O","CN1C=NC2=C1C(=O)N(C(=O)N2C)C","CC(C)Cc1ccc(cc1)C(C)C(=O)O","C1=CC2=C(C=C1O)C(=O)C3=C(O2)C=C(C=C3)O","CC(=O)Nc1ccc(O)cc1","CN1CCC[C@H]1c1cccnc1","COc1cc2c(cc1OC)C(=O)C(CC1CCN(C)CC1)C2","CC1=CC(=O)C=CC1=O","O=C(O)c1ccccc1","CCN(CC)CCNC(=O)c1cc(Cl)c(N)cc1OC","Clc1ccccc1","CC(C)NCC(O)COc1cccc2ccccc12","COc1ccc2[nH]c(nc2c1)S(=O)Cc1ncc(C)c(OC)c1C","CC(C)(C)NCC(O)c1ccc(O)c(CO)c1","Oc1ccc(cc1)C1CCNCC1","CC12CCC3c4ccc(O)cc4CCC3C1CCC2O","CN(C)CCC=C1c2ccccc2CCc2ccccc21","CCCCCCCCCCCCCCCC(=O)O","OC(=O)c1ccccc1O","Nc1ccc(cc1)S(=O)(=O)Nc1nccs1","CC(C)Cc1ccccc1"];
$('#sample-lib-btn').addEventListener('click',()=>{const file=new File([sample.join('\n')],'sample_library.smi');const dt=new DataTransfer();dt.items.add(file);$('#screen-file').files=dt.files;$('#screen-form').dispatchEvent(new Event('submit'))});
$('#screen-form').addEventListener('submit',async e=>{e.preventDefault();const input=$('#screen-file');if(!input.files.length)return;const out=$('#screen-result');out.classList.remove('hidden');out.innerHTML='<div class="loading">Running validation → Lipinski → PAINS → ML scoring → chemical-space ranking…</div>';const fd=new FormData();fd.append('file',input.files[0]);try{const r=await fetch('/api/screen',{method:'POST',body:fd});const d=await r.json();if(!r.ok){out.innerHTML=`<div class="error-box">${esc(d.detail||'Screening failed')}</div>`;return}renderScreen(d)}catch(err){out.innerHTML=`<div class="error-box">Request failed: ${esc(err)}</div>`}});
function renderScreen(d){const f=d.funnel;const funnel=`<div class="funnel">${[['submitted','Submitted'],['invalid_structures','Invalid'],['unique_valid_structures','Unique'],['passed_lipinski','Lipinski'],['paints_flagged','PAINS flagged'],['scored','Scored'],['top_candidates','Top 20']].map(([k,l])=>`<div class="funnel-step"><div class="n">${f[k]}</div><div class="lbl">${l}</div></div>`).join('')}</div>`;const cards=d.candidates.map((c,i)=>`<div class="cand-card"><div class="cand-struct">${svgImg(c.structure_svg_b64)}</div><div><div class="cand-smiles"><span class="rank-num">#${i+1}</span>${esc(c.smiles)}</div><div class="cand-tags"><span class="badge ${badgeClass(c.toxicity.label)}">Tox ${c.toxicity.label}</span><span class="badge ${badgeClass(c.solubility.label)}">Sol ${c.solubility.label}</span><span class="badge ${badgeClass(c.veber.pass?'pass':'fail')}">Veber ${c.veber.pass?'PASS':'FAIL'}</span><span class="badge ${badgeClass(c.structural_alerts.pass?'clear':'flagged')}">PAINS ${c.structural_alerts.pass?'CLEAR':'FLAGGED'}</span><span class="badge ${badgeClass(c.applicability_domain.status)}">Domain ${c.applicability_domain.status}</span></div></div><div class="cand-score"><div class="num">${c.screening_score.value}</div><div class="lbl">score</div></div></div>`).join('');$('#screen-result').innerHTML=funnel+`<div class="candidate-list">${cards||'<div class="error-box">No candidates survived the screening funnel.</div>'}</div>`}

async function loadAbout(){const out=$('#about-content');if(out.dataset.loaded)return;out.innerHTML='<div class="loading">Loading model cards…</div>';try{const [cr,tr]=await Promise.all([fetch('/api/model-cards'),fetch('/api/activity-targets')]);const d=await cr.json(),td=await tr.json();const m=d.metrics,egfr=(td.targets||[]).find(t=>t.name==='egfr'),sol=m.solubility,tox=m.toxicity;const solEval=sol.evaluation?.scaffold_split||{},toxEval=tox.evaluation?.scaffold_split||{};out.innerHTML=`<div class="model-card"><h3>Solubility — RF + Gradient Boosting</h3><div class="kv"><div>Dataset</div><div>${esc(sol.dataset)}</div><div>Holdout</div><div>${solEval.n_test??sol.n_test} molecules</div><div>Ensemble R²</div><div>${solEval.ensemble_r2??sol.ensemble_r2}</div><div>Ensemble RMSE</div><div>${solEval.ensemble_rmse??sol.ensemble_rmse} log units</div></div></div><div class="model-card"><h3>Toxicity — Tox21 NR-AR</h3><div class="kv"><div>Dataset</div><div>${esc(tox.dataset)}</div><div>Holdout</div><div>${toxEval.n_test??tox.n_test} molecules</div><div>Ensemble ROC-AUC</div><div>${toxEval.ensemble_auc??tox.ensemble_auc}</div><div>PR-AUC</div><div>${toxEval.ensemble_pr_auc??tox.ensemble_pr_auc}</div><div>F1</div><div>${toxEval.ensemble_f1??tox.ensemble_f1}</div></div></div><div class="model-card"><h3>EGFR Activity — RF + Gradient Boosting</h3><div class="kv"><div>Target</div><div>EGFR (CHEMBL203)</div><div>Dataset</div><div>${egfr?.evaluation?.n_molecules??'—'} molecules</div><div>Holdout</div><div>${egfr?.evaluation?.n_test??'—'} molecules</div><div>Scaffold overlap</div><div>${egfr?.evaluation?.scaffold_overlap??'—'}</div><div>Ensemble R²</div><div>${egfr?.evaluation?.ensemble_r2??'—'}</div><div>Ensemble RMSE</div><div>${egfr?.evaluation?.ensemble_rmse??'—'}</div></div></div><div class="scope-box"><h3>Interpretation</h3><ul class="real"><li>Model agreement = RF/GB prediction disagreement; not calibrated confidence.</li><li>Applicability domain = nearest-neighbour chemical-space proximity signal.</li><li>Screening score = hand-weighted heuristic; not a validated scientific endpoint.</li><li>Feature importance is global model-level importance, not causal per-molecule attribution.</li></ul><h3>Limitations</h3><ul class="roadmap">${(d.limitations||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div>`;out.dataset.loaded='1'}catch(e){out.innerHTML=`<div class="error-box">Could not load model cards: ${esc(e)}</div>`}}

$('#activity-form').addEventListener('submit',async e=>{e.preventDefault();const smiles=$('#activity-smiles').value.trim(),target=$('#activity-target').value,out=$('#activity-result');if(!smiles){out.classList.remove('hidden');out.innerHTML='<div class="error-box">Please enter a SMILES string.</div>';return}out.classList.remove('hidden');out.innerHTML='<div class="loading">Running target-specific activity model…</div>';try{const r=await fetch('/api/predict-activity',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({smiles,target})});const d=await r.json();if(!r.ok){out.innerHTML=`<div class="error-box">${esc(d.detail||'Prediction failed.')}</div>`;return}out.innerHTML=`<div class="metric-grid">${metric('Target',esc(d.target.toUpperCase()))}${metric('Predicted pIC50',d.predicted_pIC50)}${metric('Predicted IC50',`${d.predicted_IC50_nM} nM`)}${metric('RF pIC50',d.rf_pIC50)}${metric('GB pIC50',d.gb_pIC50)}${metric('Model agreement',`${esc(d.model_agreement.level)} · Δ ${d.model_agreement.absolute_disagreement}`)}</div><div class="section-label">Interpretation</div><div class="tiny">${esc(d.model_agreement.interpretation)}</div><div class="tiny">${esc(d.note)}</div>`}catch(err){out.innerHTML=`<div class="error-box">Request failed: ${esc(err)}</div>`}})
